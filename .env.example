# Copy this to .env for local development
# On Railway, set these in the Variables tab

# Required: Dropbox long-lived access token
DROPBOX_ACCESS_TOKEN=your_dropbox_access_token_here

# Optional: Dropbox folder path for your Obsidian vault's Brain folder
BRAIN_FOLDER=/Brain

# Optional: Port override (Railway sets PORT automatically)
# PORT=8080
